{
  "name": "ids-test",
  "version": "1.0.0",
  "description": "InDesign Server example app",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "soap": "^0.23.0"
  }
}
